# Secrets and local configuration
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Generated experiment material (kept locally unless deliberately released)
data/raw/
data/processed/
artifacts/*
!artifacts/.gitkeep
# Deliberately released: the full study this project's report/paper is based
# on (docs/experiment-plan.md v0.3). See paper/paper.pdf for the write-up.
!artifacts/study-20260917T110656Z/
logs/

# LaTeX build artifacts (paper/ keeps only .tex source, .pdf output, figures)
paper/*.aux
paper/*.log
paper/*.out
paper/*.bbl
paper/*.blg
paper/*.fls
paper/*.fdb_latexmk
paper/*.synctex.gz

# Editors and operating systems
.vscode/
.idea/
.DS_Store
Thumbs.db

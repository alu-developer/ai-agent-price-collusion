# Copy this file to .env when the first real model run is ready.
# Never commit real API keys.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=

# A deliberately small safety budget for the first real pilot, in US dollars.
EXPERIMENT_MAX_COST_USD=0.10
